{
  "name": "labo-ai-setup",
  "private": true,
  "version": "0.1.53",
  "scripts": {
    "tauri": "tauri",
    "build:ui": "sass ui/styles.scss ui/styles.css --style=compressed --no-source-map",
    "build:mac": "npm run build:ui && tauri build --bundles dmg",
    "build:win": "npm run build:ui && tauri build --bundles nsis",
    "build:linux": "npm run build:ui && tauri build --bundles appimage"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.8.5",
    "sass": "^1.101.0"
  }
}
